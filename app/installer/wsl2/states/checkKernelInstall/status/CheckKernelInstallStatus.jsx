// vendor
import React from 'react';
// components
import { LoaderText } from '../../../../../components/loader/index';

const Status = () => (
    <LoaderText
      header="Checking Kernel"
      message="Making sure the kernel is installed."
    />
  );


export default Status;
